eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
#  rabbitmq:
#    cache:
#      channel:
#        checkout-timeout: 1s
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest